{
  "name": "scrollpatrol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "package": "node scripts/package.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/chrome": "^0.1.0",
    "esbuild": "^0.28.2",
    "jsdom": "^26.0.0",
    "typescript": "^5.8.0",
    "vitest": "^5.0.1"
  }
}
